% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ppm_tune.R
\name{ppm_tune}
\alias{ppm_tune}
\title{Cross-Validation for the Penalized Principal Machine Tuning Parameter}
\usage{
ppm_tune(
  x,
  y,
  loss = "lssvm",
  d = 2,
  H = 10,
  C = 1,
  gamma = 3.7,
  penalty = c("grSCAD", "grLasso", "grMCP"),
  max.iter = 100,
  n.fold = 5,
  lambda = NULL,
  nlambda = 20,
  lambda.max = 0.1,
  lambda.min.ratio = 0.001,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{x}{A numeric matrix or data frame of predictors, of dimension
\code{n} (observations) by \code{p} (variables).}

\item{y}{A response vector of length \code{n}. For continuous-type losses a
numeric vector is expected; for weighted losses (those whose name starts
with \code{"w"}) a two-class response is expected and is recoded internally to
\eqn{\{-1, +1\}}.}

\item{loss}{Character string selecting the loss function. One of
\code{"lssvm"}, \code{"wlssvm"}, \code{"svm"}, \code{"wsvm"}, \code{"l2svm"}, \code{"wl2svm"},
\code{"logit"}, \code{"wlogit"}, \code{"asls"}, \code{"qr"}. Default \code{"lssvm"}.}

\item{d}{Working structural dimension used to form the held-out sufficient
predictors. Default \code{2}.}

\item{H}{Number of cutoffs (slices); a single integer \verb{>= 2}. Default \code{10}.}

\item{C}{Positive cost parameter that balances the loss against the
covariance term. Default \code{1}.}

\item{gamma}{Concavity parameter of the SCAD/MCP penalty; must exceed \code{2}.
Default \code{3.7}.}

\item{penalty}{Penalty type: \code{"grSCAD"} (default), \code{"grLasso"} or \code{"grMCP"}.}

\item{max.iter}{Maximum number of GCD iterations. Default \code{100}.}

\item{n.fold}{Number of cross-validation folds. Default \code{5}. For
reproducible folds, call \code{\link[=set.seed]{set.seed()}} before \code{ppm_tune()}.}

\item{lambda}{Optional numeric vector of candidate values. If \code{NULL}
(default), a log-spaced grid of length \code{nlambda} is built from
\code{lambda.max} down to \code{lambda.max * lambda.min.ratio}.}

\item{nlambda}{Length of the automatically generated grid. Default \code{20}.}

\item{lambda.max}{Largest candidate value in the generated grid. Default \code{1}.}

\item{lambda.min.ratio}{Ratio of the smallest to the largest candidate in
the generated grid. Default \code{1e-3}.}

\item{verbose}{Logical; if \code{TRUE}, progress is reported via \code{\link[=message]{message()}}.
Default \code{FALSE}.}

\item{...}{Additional arguments passed to the underlying solver. The most
useful is \code{ridge}, a small non-negative ridge constant added for numerical
stability, which is accepted by the iterative solvers
(\code{logit}, \code{wlogit}, \code{svm}, \code{wsvm}, \code{qr}, \code{lssvm}, \code{wlssvm}, \code{wl2svm}).}
}
\value{
An object of S3 class \code{"ppm_tune"}, a list with elements
\describe{
\item{opt.lambda}{the selected value of \code{lambda}.}
\item{lambda}{the candidate grid (decreasing).}
\item{dcor}{the average held-out distance correlation at each candidate.}
\item{fit}{a \code{\link[=ppm]{ppm()}} object refitted on all data at \code{opt.lambda}.}
\item{d, loss, penalty, n.fold, call}{the settings and the matched call.}
}
}
\description{
Selects the sparsity parameter \code{lambda} of a penalized principal machine by
\eqn{K}-fold cross-validation. For each candidate \code{lambda}, the model is
fitted on the training folds and the held-out distance correlation (dCor;
Szekely, Rizzo and Bakirov, 2007) between the response and the predictors
projected onto the estimated \code{d}-dimensional central subspace is recorded.
The \code{lambda} maximizing the average held-out dCor is returned, following the
criterion of Shin, Wu, Zhang and Liu (2017).
}
\examples{
\donttest{
set.seed(1)
n <- 1000; p <- 10
x <- matrix(rnorm(n * p), n, p)
y <- x[, 1] / (0.5 + (x[, 2] + 1)^2) + 0.2 * rnorm(n)
cv <- ppm_tune(x, y, loss = "lssvm", d = 2, n.fold = 5)
cv$opt.lambda
summary(cv$fit, d = 2)
}

}
\references{
Shin, S. J., Wu, Y., Zhang, H. H. and Liu, Y. (2017) Principal weighted
support vector machines for sufficient dimension reduction in binary
classification. \emph{Biometrika}, 104(1), 67--81. \doi{10.1093/biomet/asw057}

Szekely, G. J., Rizzo, M. L. and Bakirov, N. K. (2007) Measuring and testing
dependence by correlation of distances. \emph{The Annals of Statistics}, 35(6),
2769--2794. \doi{10.1214/009053607000000505}
}
\seealso{
\code{\link[=ppm]{ppm()}}
}
