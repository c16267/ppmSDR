% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{summary.ppm}
\alias{summary.ppm}
\title{Summarize a fitted penalized principal machine}
\usage{
\method{summary}{ppm}(object, d = 2, tol = 1e-06, ...)
}
\arguments{
\item{object}{An object of class \code{"ppm"} returned by \code{\link[=ppm]{ppm()}}.}

\item{d}{Working structural dimension; the number of leading eigenvectors
to report. Default \code{2}.}

\item{tol}{Tolerance below which a row L2-norm is treated as zero when
determining the selected variables. Default \code{1e-6}.}

\item{...}{Currently ignored.}
}
\value{
Invisibly, a list with elements \code{d}, \code{basis} (the \code{p} by \code{d}
estimated basis), \code{selected} (indices of the selected predictors) and
\code{n.selected}.
}
\description{
Reports the estimated basis of the central subspace for a given working
dimension \code{d}, together with the predictors selected by the row-group
penalty (those whose loadings are non-zero across the leading \code{d}
directions).
}
\examples{
set.seed(1)
x <- matrix(rnorm(200 * 6), 200, 6)
y <- x[, 1] / (0.5 + (x[, 2] + 1)^2) + 0.2 * rnorm(200)
fit <- ppm(x, y, loss = "lssvm", lambda = 0.01)
summary(fit, d = 2)
}
\seealso{
\code{\link[=ppm]{ppm()}}, \code{\link[=print.ppm]{print.ppm()}}
}
